# Haar Cascade Models

This directory is used for Haar cascade XML files:
- `haarcascade_frontalface_default.xml`
- `haarcascade_eye.xml`

On first backend startup, files are copied automatically from OpenCV's local installation into this folder so the app stays fully offline.
